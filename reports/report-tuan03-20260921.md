# Report Tuần 3 — Nhận diện chữ số viết tay (AI Project 1/3)

**Ngày:** 21/09/2026
**Giai đoạn:** Phase 1 (validation set) + Phase 1.5 (hiểu kiến trúc) + Phase 1.7 (thu gọn kiến trúc)

---

## 1. Kết quả / tiến độ

### 1.1 Thêm validation set (theo yêu cầu Thầy ở feedback tuần 2)

Tách `60.000` ảnh train thành **50k train / 10k val**, giữ nguyên 10k test. Sửa vòng train để theo dõi trên val, **test set chỉ chạm đúng 1 lần** ở cuối cùng.

Kèm theo: sửa lỗi lưu checkpoint. Trước đây file `.pth` luôn giữ trọng số epoch cuối; nay lưu theo **val accuracy cao nhất**.

### 1.2 Thu gọn kiến trúc (theo yêu cầu Thầy: 206.922 params là quá nhiều)

Xây `SmallCNN` — 3 lớp conv + 3 lớp pool + 1 lớp Dense đi thẳng ra 10 lớp, không có FC ẩn, không Dropout. Train cùng seed, cùng optimizer, cùng cách chia dữ liệu với model cũ.

| | DigitCNN (tuần 1) | SmallCNN (mới) |
|---|---|---|
| Params | 206.922 | **5.018** |
| Bộ nhớ float32 | 808,3 KB | **19,6 KB** |
| Bộ nhớ int8 (ước tính) | 202,1 KB | **4,9 KB** |
| Val accuracy | 98,96% | 98,79% |
| **Test accuracy** | **99,05%** | **98,82%** |
| Epoch tốt nhất | 7/8 | 27/30 |

**Đánh đổi: giảm 41,2 lần params, mất 0,23% accuracy** — tức giữ 99,8% hiệu năng với 2,4% bộ nhớ.

Phân bố params cũng đổi hẳn: lớp nặng nhất của DigitCNN là `fc1` chiếm **97,06%**; của SmallCNN là `conv3` chỉ **46,23%**. Không còn nút thắt bộ nhớ ở một lớp duy nhất.

**→ Chốt dùng `SmallCNN` cho các bước sau.**

### 1.3 Hiểu sâu operator (Phase 1.5)

Đã viết tài liệu cho **toàn bộ 6 operator trên đường inference**, mỗi file theo cùng 7 mục: vai trò → công thức kèm cách suy ra → mã giả vòng lặp thuần → số cụ thể của `SmallCNN` → mức FPGA-friendly → bẫy khi port sang C → câu hỏi tự kiểm tra.

| Operator | File | Params | Phép tính / ảnh | FPGA |
|---|---|---|---|---|
| `Conv2d` | `operators/conv2d.md` | 3.568 | 395.136 MAC | ✅ Rất tốt |
| `ReLU` | `operators/relu.md` | 0 | 10.192 so sánh | ✅ Tốt nhất |
| `MaxPool2d` | `operators/maxpool2d.md` | 0 | 7.488 so sánh | ✅ Rất tốt |
| `Flatten` | `operators/flatten.md` | 0 | 0 | ✅ Miễn phí |
| `Linear` | `operators/linear.md` | 1.450 | 1.440 MAC | ⚠️ Nghẽn băng thông |
| `argmax` | `operators/argmax.md` | 0 | 9 so sánh | ✅ Gần như miễn phí |
| | **Tổng** | **5.018** | ~414.000 | |

`operators/README.md` là bản index: sơ đồ đường inference 12 bước, bảng trên, 3 bẫy nguy hiểm nhất khi port sang C.

**Đường inference chỉ có 6 operator này.** Không có softmax, không có loss function, không có optimizer — những thứ đó chỉ tồn tại lúc train. Đây là danh sách đầy đủ những gì golden model C phải hiện thực.

---

## 2. Vấn đề hiện tại

1. **`CrossEntropyLoss` chưa viết tài liệu riêng.** Nhưng đã xác định được nó **không nằm trên đường inference** — chỉ tồn tại lúc train, golden model C không cần. Hệ quả duy nhất chạm tới chip đã ghi vào `operators/argmax.md`: `nn.CrossEntropyLoss` gộp sẵn `LogSoftmax` bên trong, nên `forward()` xuất logit thô và **không cần thêm lớp softmax nào**. Vì vậy em hạ mục này xuống ưu tiên thấp, không coi là chặn Phase 2A.
2. **Class model bị chép ở 4 file** (`mnist_digit_recognition.py`, `demo_app.py`, `error_analysis.py`, `model_comparison.py`). Đổi kiến trúc là phải sửa 4 chỗ, quên 1 chỗ sẽ lỗi shape hoặc tệ hơn là chạy được nhưng sai.
3. **`demo_app.py` và `error_analysis.py` vẫn chạy model cũ** — chưa đổi sang `SmallCNN`.
4. **Chưa rõ ngưỡng accuracy Thầy chấp nhận.** Em đang không biết nên ưu tiên params nhỏ nhất có thể, hay phải giữ một sàn accuracy nào đó.

---

## 3. Giải pháp đã áp dụng & kiến thức mới

### Giải pháp

| Vấn đề | Cách xử lý |
|---|---|
| Data leakage (dùng test set theo dõi lúc train) | Tách validation set riêng, test set chỉ chạm 1 lần cuối |
| Checkpoint không khớp epoch tốt nhất | So val acc mỗi epoch, chỉ ghi đè file khi phá kỷ lục |
| So sánh 2 kiến trúc không công bằng | Cố định seed, dùng chung một cách chia dữ liệu cho cả hai |
| Không biết model đã hội tụ chưa | Script tự cảnh báo khi epoch cuối vẫn là epoch tốt nhất |

### Kiến thức mới

**a) Cái gì thật sự giảm params.** Data augmentation và tinh chỉnh hyperparameter **không** giảm một params nào — chúng chỉ đổi giá trị trọng số. Quantization giảm số byte mỗi trọng số chứ không giảm số trọng số. **Chỉ có sửa kiến trúc mới giảm được số params.**

**b) Giảm params là ba đòn bẩy nhân với nhau**, không phải một chiêu:

| Đòn bẩy | Thay đổi | Hệ số |
|---|---|---|
| Bớt channel | 32 → 16 | 2× |
| Thêm conv3 + pool3 (ép spatial trước khi Flatten) | 7×7 = 49 → 3×3 = 9 | 5,4× |
| Bỏ lớp FC ẩn | `1568→128→10` thành `144→10` | phần còn lại |

Kết quả ở lớp FC: `200.832` → `1.450` params, ít hơn 138 lần.

**c) Params ≠ FLOPs.** Conv2d có công thức params `k×k×in_ch×out_ch + out_ch` — **không chứa kích thước ảnh**, vì cùng một kernel được dùng lại khắp ảnh. Linear thì `in_features × out_features`, mà `in_features` chính là ảnh đã duỗi phẳng. Đó là lý do conv rẻ về bộ nhớ còn fc đắt.

Ví dụ trong SmallCNN: `conv1` ít hơn `conv3` **29 lần** params nhưng chỉ ít hơn **2 lần** phép tính, vì `conv1` chạy trên ảnh 28×28 còn `conv3` chạy trên 7×7.

**d) Model nhỏ cần nhiều epoch hơn.** SmallCNN ở 8 epoch chỉ đạt val 98,42%; tới epoch 27 lên 98,79% — tăng 0,37% mà không đổi một dòng kiến trúc nào. Dùng chung một con số epoch cho hai model là so một model đã hội tụ với một model bị cắt ngang giữa chừng.

**e) Validation set chứng minh được kết quả không phải ăn may.** Test acc (98,82%) cao hơn val acc (98,79%). Val acc là giá trị lớn nhất trong 30 lần đo nên tự nó hơi lạc quan; test set chỉ chạm 1 lần và không dùng để chọn gì, mà vẫn ra cao hơn.

**f) Công thức output size và lý do đằng sau nó.**

```
H_out = floor( (H_in + 2×padding − kernel) / stride ) + 1
```

Suy ra bằng cách đếm số vị trí đặt được cửa sổ. Hệ quả: `padding = (kernel−1)/2` thì giữ nguyên kích thước — chỉ ra số nguyên khi kernel lẻ, một trong các lý do kernel luôn lẻ.

Chỗ `7 → 3` là do `floor`: hàng cuối và cột cuối (chỉ số 6) bị bỏ hoàn toàn. Với MNIST không sao vì chữ số nằm gọn trong vùng 20×20 ở giữa, nhưng khi viết C phải lặp lại đúng hành vi `floor` này, viết `ceil` là shape lệch từ đó trở đi.

---

## 4. Kế hoạch tuần tới

1. **Gom class model vào `models.py`**, sửa 3 file còn lại để import từ đó. Làm trước khi bước sang Phase 2B — hiện `DigitCNN` đang bị chép ở 4 file.
2. **Cập nhật `demo_app.py` và `error_analysis.py`** sang `SmallCNN` — chạy lại confusion matrix trên model mới để xem cặp chữ số hay nhầm có đổi không.
3. **Trích tham số ra file** (Phase 2B) — xuất 5.018 số từ `small_cnn.pth` sang định dạng C đọc được, giữ đúng layout NCHW.
4. **Bắt đầu golden model C** (Phase 2D) nếu Thầy xác nhận nó thuộc project 1 — viết từng operator theo mã giả đã có, so từng lớp với PyTorch chứ không chỉ so accuracy cuối.
5. Cập nhật file Word bước 3 với 4 operator mới. Để sau buổi gặp vì câu trả lời của Thầy có thể đổi phạm vi.

### Câu hỏi cần Thầy chốt

1. Golden model C có nằm trong project 1 (mảng AI) không, hay là đầu việc của project IC?
2. Bước "thay phép toán khó triển khai xuống FPGA": em tìm được **2 chỗ bỏ bớt được, cả hai cho kết quả giống hệt bit-for-bit chứ không phải xấp xỉ**:

   | Bỏ gì | Tiết kiệm | Vì sao đúng |
   |---|---|---|
   | Softmax ở lớp cuối | 10 phép `exp()` + 1 phép chia | `exp` đơn điệu tăng nên không đổi thứ tự → `argmax` ra kết quả y hệt |
   | Đổi thứ tự `relu` ↔ `maxpool` | 10.192 → 2.496 phép ReLU (4,08 lần) | `max` và `relu` đều đơn điệu không giảm nên hoán vị được. Chỉ đúng với MaxPool, **sai** với AvgPool |

   Ngoài 2 chỗ này, bước đó còn việc gì nữa không ạ? Kiến trúc `SmallCNN` không còn phép chia hay hàm siêu việt nào khác.
3. Golden model C được dùng `math.h` (`expf`, `sqrtf`) hay chỉ `stdio.h`/`stdlib.h`?
4. "Tối ưu trọng số khi đưa vào chip": Thầy muốn int8, int16 hay fixed-point Qm.n? FPGA mục tiêu là board nào? Làm sau khi golden model C float32 chạy đúng có được không?
5. **Ngưỡng accuracy chấp nhận được là bao nhiêu?** Em nên chạy theo params nhỏ nhất, hay phải giữ một sàn accuracy?
6. **Em có nên dùng đúng kiến trúc tham chiếu không**, hay Thầy muốn em tự thiết kế kiến trúc khác cùng ngân sách params?

---

**File liên quan:** `model_comparison.py`, `final_table.py`, `make_figures.py`, `operators/` (README + 6 operator), `study/xem_quantization.py`, `roadmap-digit-recognition-v3.md`
